# Sesión de trabajo — 2026-06-09

> Registro de trabajo realizado por **nachoDev**. Rama: `master`.

---

## Trabajo 1 — Columnas de datos personales desde `mamore`/`sysadmin` y mejoras de interfaz de usuarios

**Commits:** `1f27c17`, `f442a3b`, `6a83167`, `0e80148`, `b8a24f9`, `9b7c47a` — `actualizando las columna de los datos personales relacionado al sistema mamore y mejoras en la parte de la interfaz`

### Problema
Las columnas de datos personales (nombre, CI) mostradas en listados y en el BREAD de
usuarios no coincidían con la estructura actual de la base de personal (`mamore` /
`sysadmin`), y el formulario de usuarios en Voyager era confuso (columnas duplicadas,
mal ordenadas).

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `app/Models/User.php`, `app/Models/Unit.php` | Ajuste de atributos/relaciones para consumir los datos personales actualizados |
| `database/migrations/2026_06_09_*` (5 migraciones BREAD) | Reordenan el BREAD de usuarios en Voyager: ocultar `name`, reordenar `funcionario`, fusionar dirección/unidad/avatar y almacén/sub-almacén |
| `database/migrations/2026_06_10_000000_add_audits_bread_to_voyager.php` | BREAD de auditoría (`audits`) agregado a Voyager |
| `resources/views/almacenes/users/*-browse.blade.php` | Vistas parciales nuevas para mostrar almacén, dirección y funcionario en el listado |
| `resources/views/vendor/voyager/users/edit-add.blade.php` | Formulario de usuarios reorganizado |
| `resources/views/vendor/voyager/dashboard/sidebar.blade.php`, `navbar.blade.php`, `master.blade.php` | Limpieza y mejoras visuales del panel |
| Vistas de listados (`egress`, `income`, `inbox`, `outbox`, reportes) | Actualizadas al nuevo formato de nombre completo |
| `database/seeders/DataRowsTableSeeder.php`, `DataTypesTableSeeder.php`, `MenuItemsTableSeeder.php`, `PermissionRoleTableSeeder.php` | Seeders sincronizados con la nueva configuración BREAD |

### Solución
Se actualizó la forma de componer el nombre completo del funcionario al consumir
`sysadmin`/`mamore` y se reorganizó el BREAD de usuarios mediante migraciones (para
que el cambio sea reproducible en despliegues). Se agregó el módulo de auditoría al
menú de Voyager.

---

## Trabajo 2 — Actualización del framework a Laravel 10

**Commits:** `e71e6ce`, `1d7cd64`, `3a26d50`, `f3fadc8` — `actualizacion a laravel 10`

### Problema
El sistema corría en Laravel 8, sin soporte oficial, lo que bloqueaba actualizaciones
de seguridad y de librerías.

### Archivos modificados
| Archivo | Cambio |
|---|---|
| `composer.json` / `composer.lock` | `laravel/framework` a ^10, actualización de todas las dependencias compatibles |
| `config/app.php`, `config/audit.php`, `config/filesystems.php`, `config/voyager.php` | Ajustes de configuración requeridos por Laravel 10 |
| `public/css/dataTable.css` y vistas varias | Correcciones menores derivadas de la actualización |

### Solución
Actualización incremental en cuatro commits: dependencias, archivos de configuración
y correcciones de compatibilidad, verificando que Voyager, auditoría y los módulos de
almacén siguieran operativos.

---

## Informe para presentación (copiar/pegar en Word)

**Informe de trabajo — 09/06/2026**
**Responsable:** nachoDev

**Actividad:** Actualización de los datos personales mostrados en el sistema y modernización del framework base.

**Problema detectado:**
1) Los nombres de los funcionarios se mostraban incompletos o desordenados porque el sistema de personal cambió su estructura de datos.
2) El sistema funcionaba sobre una versión del framework sin soporte, con riesgo de seguridad.

**Solución aplicada:**
1) Se actualizó la lectura de datos personales desde el sistema de personal y se reorganizó el formulario y listado de usuarios para que sea más claro.
2) Se actualizó el sistema a Laravel 10, versión con soporte vigente.

**Detalle de archivos modificados:**
1. Modelos y vistas de usuarios: nombres completos correctos en todos los listados.
2. Formulario de administración de usuarios: reorganizado.
3. Framework y librerías: actualizados a versiones con soporte.

**Resultado:** Datos de personal correctos en toda la interfaz y plataforma actualizada y segura.

---
